apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: mysql-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi # İhtiyaca göre ayarlama işlemi
  storageClassName: standard # Gerekli storage class'a göre ayarlayın
